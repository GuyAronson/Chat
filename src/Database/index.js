// a general import to refernce the server and data base properties

import { User } from './User'
import { Chat } from './Chat';
import { Message } from './Message'
import { DataBase as Server} from './DataBase'

export const Database = {
    User,
    Chat,
    Message,
    Server
}